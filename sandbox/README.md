# Песочница UI

**Vite + React + TypeScript + Tailwind + shadcn/ui.** Тот же React, что будет в Next.js.

```bash
cd sandbox/ui
npm install
npm run dev
```

Браузер: расписание, кабинет, админка. Пункты «скоро» не кликаются. RU / EN в шапке.

Архив HTML: `legacy-html/` — не используем.
