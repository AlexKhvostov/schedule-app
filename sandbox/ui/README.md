# sandbox/ui

Живой клубный интерфейс — Vite + React. Как ставить метки: [`../../docs/schedule-ux.md`](../../docs/schedule-ux.md#работа-с-метками). Статус и переход в `apps/web`: [`../../docs/sandbox.md`](../../docs/sandbox.md).

```bash
npm install
npm run dev
```

Ключи — `.env.example` и [`../../supabase/README.md`](../../supabase/README.md). Без `.env.local` Discord на входе не настоящий.
